# A minimal setup for local debugging with Cucumber.js, Playwright and Allure.

## Prerequisites

### Node.js 18+ and Yarn 1.x installed.

#### Optional: Allure CLI if an HTML report is needed.

Install

```bash
yarn install
```
Run tests
```bash
yarn test
```
